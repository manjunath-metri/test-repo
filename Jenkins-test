pipeline{
    agent any
    
    environment{
    fname = "manjunath"
    }
    stages{
        stage("GIT checkout"){
            steps{
                git 'https://github.com/manjunath-metri/myweb.git'
                
            }
        }
        stage("Maven Build"){
            steps{
                sh "mvn clean package"
                sh "mv target/*.war target/myweb.war"
            }
         }
    }
}
